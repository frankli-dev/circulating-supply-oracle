{
  "name": "supply-api",
  "version": "1.0.0",
  "description": "circulating supply api",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cargo run",
    "up": "pm2 start target/debug/supply-api --watch",
    "down": "pm2 stop 0 --watch && pm2 delete 0"
  },
  "author": "",
  "license": "ISC"
}
